import * as assert from 'assert';

suite('GlobalState Tests', function() {
  test('Initialise ExtensionContext variable in GlobalState and check that an empty array called DataPoints has been added to the given ExtensionContext.globalState', function() {
    assert.strictEqual(2 * 5, 10);
  });
});
