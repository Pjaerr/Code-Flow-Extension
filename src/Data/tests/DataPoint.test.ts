import * as assert from 'assert';

suite('Data Point', function() {
  test('Construct a DataPoint object and check that it has received all of its values', function() {
    assert.strictEqual(2 * 6, 12);
  });
});
