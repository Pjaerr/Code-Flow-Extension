/** @type {Array} */
var DataPoints = dataPoints;
console.log(DataPoints);

const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

ctx.fillRect(canvas.width * 0.5, canvas.height * 0.5, 30, 30);
